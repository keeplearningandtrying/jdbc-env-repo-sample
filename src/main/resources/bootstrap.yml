management:
  security:
    enabled: false

spring:
  application:
    name: demo
  cloud:
    config:
      label: master
      server:
        bootstrap: true
  datasource:
    schema: classpath:schema-jdbc.sql
    data: classpath:data-jdbc.sql
  profiles:
    active: jdbc